{
  "name": "ec2-bootup",
  "version": "0.1.0",
  "bin": {
    "ec2-bootup": "bin/ec2-bootup.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.118.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.118.0",
    "constructs": "^10.0.0",
    "dotenv": "^16.3.1"
  }
}
